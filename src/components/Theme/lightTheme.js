import colors from "./ColorPalette"

const stepperButton = {
  background: colors.grey,
  content: colors.black
}

const lightTheme = {
  appBackground: colors.lightGrey,
  colors,
  fontColor: colors.black,
  stepperButton,
  header: colors.red,
  red: colors.red,
  green: colors.green,
  orange: colors.orange
}

export default lightTheme
